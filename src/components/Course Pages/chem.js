import React from 'react';
import { makeStyles } from '@material-ui/core/styles';
import Table from '@material-ui/core/Table';
import TableBody from '@material-ui/core/TableBody';
import TableCell from '@material-ui/core/TableCell';
import TableContainer from '@material-ui/core/TableContainer';
import TableHead from '@material-ui/core/TableHead';
import TableRow from '@material-ui/core/TableRow';
import Paper from '@material-ui/core/Paper';

const useStyles = makeStyles({
  table: {
    minWidth: 650,
  },
});

function createData(name, IGP10, IGP90, grad_sal2020, RankPoint) {
  return { name, IGP10, IGP90, grad_sal2020, RankPoint};
}

const rows = [
  createData('Chemistry', 'AAA/C', 'AAA/A', '$3,528', 82.5),
];

export default function Chem() {
  const classes = useStyles();

  return (
    <TableContainer component={Paper}>
      <Table className={classes.table} aria-label="simple table">
        <TableHead>
          <TableRow>
            <TableCell>Course Name</TableCell>
            <TableCell align="right">IGP(10th Percentile)</TableCell>
            <TableCell align="right">IGP(90th Percentile)</TableCell>
            <TableCell align="right">Median Salary</TableCell>
            <TableCell align="right">Rank Points</TableCell>
          </TableRow>
        </TableHead>
        <TableBody>
          {rows.map((row) => (
            <TableRow key={row.name}>
              <TableCell component="th" scope="row">
                {row.name}
              </TableCell>
              <TableCell align="right">{row.IGP10}</TableCell>
              <TableCell align="right">{row.IGP90}</TableCell>
              <TableCell align="right">{row.grad_sal2020}</TableCell>
              <TableCell align="right">{row.RankPoint}</TableCell>
            </TableRow>
          ))}
        </TableBody>
      </Table>
      <div style={{marginLeft: 45, width: '80%'}}>
      
     
An undergraduate degree in Chemistry prepares students for careers as qualified chemists. It also provides a foundation for careers in many other diverse fields. Its curriculum is grounded in both experimental applications and theoretical studies. It also offers breadth and depth, covering fundamental knowledge of the major fields of chemistry, namely: analytical; inorganic; organic and physical chemistry, which can be applied to many scientific fields.

You will also focus on higher chemistry learning in various multidisciplinary subjects including nanotechnology, chemical biology, medicinal chemistry, materials chemistry, energy and environmental sciences as well as advanced research laboratory training.
    

      </div>
      <div style={{marginLeft: 45, width: '80%'}}>
      You can find more information in https://chemistry.nus.edu.sg/
      </div>
    </TableContainer>
  );
}